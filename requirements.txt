# Jarvis-v2 requirements. Every package has a Python 3.14 Windows wheel on PyPI
# (checked 2026-09-16): py3-none-win_amd64 for vosk/sounddevice, pure-Python
# wheels for the rest. No pygame (it has no 3.14 wheel).
pyttsx3==2.99        # Windows SAPI text-to-speech (pure Python; uses comtypes on Windows)
vosk==0.3.45         # offline speech recognition
sounddevice==0.5.6   # microphone input (bundles PortAudio)
pynput==1.8.2        # keyboard control + media keys for volume
requests==2.34.2     # Groq + Fish Audio HTTP calls
pyyaml==6.0.3        # config.yaml / local.yaml
